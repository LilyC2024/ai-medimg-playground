[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-medimg-playground"
version = "0.2.0"
description = "Day 1-2 DICOM ingestion, preprocessing, and diagnostics workflow."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "matplotlib",
  "numpy",
  "opencv-python",
  "pandas",
  "pydicom",
  "scikit-image",
  "scipy",
  "SimpleITK",
  "tqdm",
]

[project.optional-dependencies]
ml = [
  "fastapi",
  "monai",
  "onnxruntime",
  "torch",
  "uvicorn",
]
